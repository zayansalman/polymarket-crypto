{
  "_paused": "gen_docs hooks paused during chore/cleanup-dead-code (2026-09-21) — every edit re-running gen_docs.py --fast against a tree mid-deletion/mid-move is noise, and gen_docs.py --check on Stop will be red for the whole rebuild by design. Restore the 'hooks' block below (see git history of this file) once the rebuild lands and gen_docs's relative-import bug (Stage F of the cleanup plan) is fixed.",
  "hooks": {}
}
